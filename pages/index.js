import Head from "next/head";
import { BurgerMenu } from "../components/BurgerMenu";
import { DailyPhrase } from "../components/DailyPhrase";
import { PlusIcon } from "../components/PlusIcon";
import { Search } from "../components/Search";
import styles from "../styles/Home.module.css";

export default function Home() {
  return (
    <div className="flex flex-col h-screen justify-between">
      <Head>
        <title>Repuestos Climatización</title>
        <meta name="description" content="Generated by create next app" />
        <link rel="icon" href="/favicon.ico" />
      </Head>

      <header className="flex flex-row justify-between items-center">
        <DailyPhrase />
      </header>

      <main className="flex flex-col items-center justify-center flex-1 px-10 text-center">
        <div className={styles.title}>
          <h1>Bienvenido </h1>
          <h1 className="font-extrabold text-transparent bg-clip-text bg-gradient-to-bl from-pink-500 to-yellow-500">
            Nombre
          </h1>
        </div>

        <h3 className="text-2xl m-14">¿Que deseas buscar?</h3>
        <Search />
      </main>
      <footer className="w-full h-24 static bottom-0 mb-10">
        <div className="flex justify-center space-x-48">
          <PlusIcon />
          <BurgerMenu />
        </div>
      </footer>
    </div>
  );
}
